# ============ 农作物病害智能识别与诊断系统 - 云端部署依赖 ============
# Python 版本要求：3.10+
# 注意：本配置不含 torch，应用将自动进入演示模式（模拟预测）
# 如需真实模型推理，请使用 requirements_full.txt

# ============ Web 应用框架 ============
streamlit>=1.32.0

# ============ 数据处理与可视化 ============
numpy>=1.24.0
pandas>=2.0.0
Pillow>=10.0.0
plotly>=5.17.0

# ============ 大语言模型接口 ============
openai>=1.6.0          # 通义千问 / OpenAI 兼容接口
requests>=2.31.0       # Ollama 本地模型调用（云端不使用）

# ============ 实用工具 ============
tqdm>=4.66.0
